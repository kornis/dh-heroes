// Require de Express
const express = require('express');
// require del metodo router
const router = express.Router();

router.get('/',callback);
router.get('/creditos',callback)

module.exports = router;